// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://bolmsuicideprevention.com',
  base: '/',
  outDir: './dist',
  publicDir: './public',
});
