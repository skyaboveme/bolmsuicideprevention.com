---
// Floating Crisis Button - Always visible for quick access to help
---

<div class="crisis-button-container">
    <a href="tel:988" class="crisis-button" aria-label="Call 988 Crisis Lifeline">
        <span class="crisis-icon">🆘</span>
        <span class="crisis-text">Crisis? Call 988</span>
    </a>
</div>

<style>
    .crisis-button-container {
        position: fixed;
        bottom: 2rem;
        right: 2rem;
        z-index: 9999;
    }

    .crisis-button {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 1rem 1.5rem;
        background: var(--crisis-red);
        color: white;
        text-decoration: none;
        border-radius: var(--radius-full);
        box-shadow: 0 4px 20px rgba(220, 53, 69, 0.4);
        font-weight: 700;
        font-size: 1.1rem;
        transition: all 0.3s ease;
        animation: pulse 2s infinite;
    }

    .crisis-button:hover {
        transform: scale(1.05);
        box-shadow: 0 6px 30px rgba(220, 53, 69, 0.6);
        animation: none;
    }

    .crisis-icon {
        font-size: 1.5rem;
        animation: shake 3s infinite;
    }

    @keyframes pulse {
        0%, 100% {
            box-shadow: 0 4px 20px rgba(220, 53, 69, 0.4);
        }
        50% {
            box-shadow: 0 4px 30px rgba(220, 53, 69, 0.7);
        }
    }

    @keyframes shake {
        0%, 90%, 100% {
            transform: rotate(0deg);
        }
        92%, 96% {
            transform: rotate(-10deg);
        }
        94%, 98% {
            transform: rotate(10deg);
        }
    }

    @media (max-width: 768px) {
        .crisis-button-container {
            bottom: 1rem;
            right: 1rem;
        }

        .crisis-button {
            padding: 0.75rem 1rem;
            font-size: 1rem;
        }

        .crisis-text {
            font-size: 0.9rem;
        }
    }

    @media (max-width: 480px) {
        .crisis-button {
            padding: 0.75rem;
            gap: 0.5rem;
        }

        .crisis-text {
            display: none;
        }

        .crisis-icon {
            font-size: 1.8rem;
        }
    }

    @media print {
        .crisis-button-container {
            display: none;
        }
    }
</style>
